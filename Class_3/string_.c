#include <stdio.h>
#include <string.h>

int main()
{
    char str[100000];
    // char space[1] =" " ;
    // char str2[100];

    // scanf("%s", &str);
    gets(str);
    // scanf("%s", &str2);

    // // strcat(str, str2);
    // strcpy(str, str2);

    // // printf("%c", str[9]);

    printf("%s", str);
}